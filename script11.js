// Operador condicional ternário

// dependendo da condição, nós recebemos valores diferentes

// Condição então valor 1 se não for valor 2
// condition ? valor 1 : valor 2

let pao = true
let queijo = true

const niceBreakfast = pao && queijo ? "Café ótimo" : "dá pro gasto"

console.log(niceBreakfast)



// Maior de 18
let age = 16
const canDrive = age >= 18 ? "you can drive" : "you can't drive!"
console.log(canDrive)